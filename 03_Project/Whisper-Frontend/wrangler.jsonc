{
  "name": "whisper-web",
  "compatibility_date": "2026-09-21",

  // Static export output (see scripts/build-static.sh). Build with
  // `bash scripts/build-static.sh`, then deploy with `npx wrangler deploy`.
  "assets": {
    "directory": "./out",
    "not_found_handling": "404-page"
  }
}
