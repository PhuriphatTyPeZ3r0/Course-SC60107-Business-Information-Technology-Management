# Copy this file to `.env` (same folder as docker-compose.yaml) and fill in
# real values. `.env` is gitignored - never commit real tokens.

# Required only for mono-file (1 channel) speaker separation via /diarize.
# Stereo files, /transcribe, and /summarize all work fine without this.
# Get one at https://huggingface.co/settings/tokens, and before it will work
# you must accept the license on these two pages while logged in:
#   https://huggingface.co/pyannote/speaker-diarization-3.1
#   https://huggingface.co/pyannote/segmentation-3.0
HF_TOKEN=
